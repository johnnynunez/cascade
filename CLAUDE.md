# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

CASCADE (repo: `github.com/johnnynunez/cascade`; Python package/import path and CLI entry points are unchanged — still `cascade`/`wrc-*` everywhere, see README's "Naming note") is LLM-orchestrated tabletop manipulation, robot- and device-agnostic by design: 10 arm profiles spanning 5/6/7 DoF over CAN, a Feetech serial bus, MuJoCo and Isaac, and any accelerator or none. The reference rig is a Seeed reBot DevArm B601 (RobStride) driven from an NVIDIA DGX Spark — treat that as *one* configuration, not the subject. Read `README.md` (user-facing overview, quick start, safety notes) and `docs/ARCHITECTURE.md` (module map, design decisions) first — this file covers what they don't, and flags where they are stale (see "Doc status" at the bottom).

## Environment & commands

On the reference rig there is no bare `python` and `python3` has no pytest, so everything runs through a uv venv. The interpreter path is machine-specific — derive it, never hardcode it:

```bash
source "$(cd .. && pwd)"/.demo/bin/activate    # reference rig layout
# or, from a fresh clone anywhere:
uv venv && uv pip install -e '.[dev,kinematics]' && source .venv/bin/activate

python -m pytest tests/ -q                             # full suite (737 passed / 2 hardware auto-deselected / 0 skipped, ~4 min with the sim extras)
python -m pytest tests/test_safety.py::test_velocity_cap -q    # single test
python -m pytest tests/ -m hardware -q                 # live-rig tests (L515 + can0, read-only)

# offline wiring check: mock camera + mock arm + scripted LLM, no hardware, no GPU
python -m cascade.apps.demo --task "pick and place pink object"
python -m cascade.apps.demo --interactive --no-view --no-serve   # REPL, no GUI/dashboard

# same cascade on the 5-DoF SO-101. `mock_small` is the matching scene: the
# default `mock` box is 7 cm across and a 55 mm jaw correctly refuses it, so
# `--camera mock` on this arm only ever exercises the rejection path.
python -m cascade.apps.demo --arm so101_mock --camera mock_small --task "pick and place the red object"
```

- Tests need no `PYTHONPATH` (`tests/conftest.py` inserts `src/`). The package is installed editable in the shared venv, so apps run without it too; `PYTHONPATH=src` (as in the README) is only needed with an interpreter that lacks the editable install.
- `addopts = "-m 'not hardware'"` in pyproject applies even to explicitly named files: `pytest tests/test_hardware_smoke.py` runs **zero tests silently** — you must add `-m hardware`.
- Pinocchio-dependent tests skip via `needs_pin` from conftest when `pin` or the RS URDF under `assets/` is missing. The SO-101 equivalent is `needs_pin_so101` — its URDF *is* vendored, so in practice only `pin` gates it. Pinocchio is not really optional: FK/IK back the safety layer, so a minimal install is base deps + `kinematics`.
- CI is a 3-OS matrix (`ubuntu-latest`, `ubuntu-24.04-arm`, `macos-latest`; Python 3.12) running `uv sync --extra dev --extra kinematics --extra arm-feetech --extra sim && uv run pytest tests/ -q`, plus a `minimal-install` job that asserts torch/ultralytics/mujoco/pyserial/openai are ABSENT and still runs a full SO-101 mock episode. The aarch64 leg is the Jetson architecture; the macOS leg is the MPS path. There is no lint step.
- MuJoCo tests need fetched assets as well as the package: `python scripts/fetch_robot_assets.py so101` (~17 MB, gitignored). Without them `tests/test_mujoco_arm.py` skips its physics half with a message naming the command; the profile/name-mapping half always runs.
- `scripts/setup_env.sh` defaults its interpreter to a stale absolute path from the original dev box — pass `PY=<interpreter>`. The venv has no `pip` module; use `uv pip`.

## Architecture

`docs/ARCHITECTURE.md` has the module map and data-flow diagram. The load-bearing facts beyond it:

**Composition root.** `apps/demo.py:build_runtime()` is the single place everything is wired: kinematics → LazyArm → SafetyHarness/SafeArm → ArmRig → CameraRig of CameraStreams → LockedDetector → WorldWatcher → BeliefStore/EpisodicMemory → TraceLogger → SkillRuntime → optional StreamServer/RigViewer. The demo CLI, `apps/mcp_server.py`, and `scripts/dashboard_runner.py` all call it; tear down with `shutdown_runtime()`. Detector selection: only the FIRST/manipulation camera's profile can override the global `detector:` in `configs/demo.yaml` — one shared `LockedDetector` serves the whole rig, and `detector:` blocks on non-primary camera profiles are silently ignored (the mock camera pins `type: mock` so offline runs never import ultralytics). The first camera in any `--cameras`/`CASCADE_CAMERAS` list is the manipulation camera; the rest only stream/fuse. A camera fuses 3D beliefs only if its profile has an `extrinsics:` block (or `fuse_beliefs: true`).

**The arm rig (`control/arm_rig.py`, added 2026-08-28).** `ArmRig` is the deliberate twin of `CameraRig`: N named arms, first = manipulation arm, `get(None)` returns the primary. `--arms a,b` (or `arms=[...]` in `load_demo_config`) builds it; `--arm a` is the one-arm case and is byte-identical to the pre-rig behaviour (pinned by `tests/test_arm_rig.py`). `runtime.arm` still binds to the primary `SafeArm`, so the skill layer's 56 `self.arm` uses were untouched; the rig hangs off `runtime.arm_rig`.

Each arm gets its OWN `SafetyHarness`. That is the whole point: every limit in `SafetyLimits` (workspace box, velocity cap, joint margins, table height) is a property of a particular robot on a particular table, so merging every arm's `overrides:` into one global config would let the LAST profile loaded define the envelope for BOTH. `load_demo_config` therefore applies only the PRIMARY arm's overrides at the top level and stores each arm's own view under `cfg.arms[i].resolved`, which is what `_build_arm` hands to that arm's harness. The `occupancy` map is the one shared object (it describes the world, not the robot, and the WorldWatcher only refreshes one — a per-arm copy would age out and silently stop gating).

**Addressing an arm from a skill call.** Every skill in `_MOTION_SKILLS` takes an optional `arm="<name>"`; `list_arms` reports the names. It is resolved in `SkillRuntime.execute()` — the single choke point — which pops `arm` from the kwargs and rebinds `self.arm` for exactly that call, so **no skill signature changed** (all 56 `self.arm` uses still read the right robot). `SkillRuntime.arm` is therefore a property over a thread-local override: the MCP server answers stop frames on a reader thread while a skill runs on a worker, and a plain attribute would let one thread's arm selection leak into another's. The override is restored in a `finally`, so a failed motion on the second arm cannot leave later skills pointed at it. The `arm` schema entry is INJECTED over `_MOTION_SKILLS` at the bottom of `runtime.py` rather than hand-written into 15 schemas — dispatch already derives the set from `_MOTION_SKILLS`, and a hand-copied list would drift the moment a motion skill is added. An unknown name is a `SkillError` (`{"ok": false}`), never a silent fallback to the primary: on a shared table that would be a collision, not a wrong answer.

**Inter-arm collision (`safety/harness.py`, added 2026-08-28).** Solved for the geometry, not for meshes. An arm profile may declare `base_pose: [x,y,z,r,p,y]` — where that robot is bolted, in a SHARED TABLE FRAME (`types.pose_to_transform`, URDF `<origin rpy=...>` convention). That is the enabling piece: every other position in this codebase is in the ROBOT BASE frame, so two arms' links are not comparable until both are lifted into one frame. RPent (PR #98) solves the same problem by declaring one arm's base canonical; a table frame is the same idea without privileging a robot. With `base_pose` on two arms, `_wire_neighbors` (demo.py) gives each harness a *callable* per neighbour; `approve()` then gates every 50 Hz waypoint on measured link distance, and `vet_pose()` applies the same gate pre-motion so a doomed grasp candidate loses at ranking time. (`safety.neighbor_clearance_m` defaults to 0.05 m — see below for why that number, and why it used to be 0.10.)

Three properties that are load-bearing and easy to break: (1) the distance is SEGMENT-to-segment on link centrelines (`safety/geometry.py`, clamped-parameter closed form, vectorised), NOT point-to-point on joint origins — the point version had a real blind spot: measured over 4000 random pose pairs it overestimates clearance by up to 3.3 cm, e.g. reporting 6.6 cm for links that are 3.3 cm apart, which a 5 cm gate then accepts. What stays unmodelled is link *shape*, which is what `neighbor_clearance_m` (default 0.05 m) stands in for; (2) an unreadable neighbour degrades to SKIP, never to "blocked" (booth rule, shared with the occupancy map) — the only such case here is a standby LazyArm, because reading its joints would power the motors from inside a safety check, which is also why the neighbour is a callable and not an arm object; (3) with no `base_pose` and no neighbours the check returns before touching kinematics, so a single-arm rig pays nothing. Cost when it does run: 77 µs per waypoint, 0.4% of the 50 Hz budget. The shipped `so101_left`/`so101_right` sit 0.44 m apart and deliberately SHARE workspace — at 0.60 m they never come closer than 0.218 m and the check would be decorative. `keep_out` is no longer the separation mechanism for these profiles.

**Camera calibration (`perception/calibration.py` + `scripts/calibrate_camera.py`, added 2026-08-28).** `Extrinsics` consumed a `T_cam2base` that nothing in-repo produced. Now `fit_extrinsic()` recovers it by Kabsch from point correspondences — the arm is its own target: FK gives the base-frame point exactly, depth gives the camera-frame one (RPent PR #29 does the same, detecting the jaw by toggling the gripper). **A rigid fit never fails, it only fails quietly**, so the result carries `rmse_m` AND a `spread_m` degeneracy check, and `.acceptable` gates both; `load_extrinsic` refuses a rejected record by default. The traps, both measured on this arm: coplanar samples give a **0 mm residual on a wrong transform** (only `spread_m` catches it), and `MIN_SPREAD_M` is 0.015 because the SO-101's reachable top-down band is only ~0.06 m tall — 0.03 would reject every real calibration. `_collect` tries a radial tool yaw *and* home's orientation per target, because the fixed orientation alone solved 13/80 poses all at y=0 (a degenerate cloud); trying both gives 44/80 and recovers a known extrinsic to 0.31 mm / 0.094°. `--self-test` exercises all of it offline and moves nothing.

**Skill system (the main extension point).** No registry: `SkillRuntime.execute()` in `src/cascade/skills/runtime.py` dispatches to `skill_<name>` methods by name, and `TOOL_SPECS` at the bottom of the same file is a hand-maintained list of JSON schemas. To add a skill: (1) write `skill_<name>()` returning a dict — raise `SkillError`/`SafetyViolation` for failures, never let other exceptions escape by design (`execute()` converts everything to `{"ok": false, ...}`); (2) add a `TOOL_SPECS` entry — `tests/test_llm_and_library.py` asserts the mapping in BOTH directions (every spec has a `skill_` method, every `skill_*` method has a spec, every `_MOTION_SKILLS` name is real) and pins README's headline skill/tool counts to the derived numbers, so forgetting the entry or the README line fails the suite; (3) **if it moves the arm, add it to `_MOTION_SKILLS` (top of runtime.py)** — this is what pauses WorldWatcher belief fusion during motion AND what records the Vesta memory frame + verdict afterwards; forgetting it re-registers the held object at bogus mid-air positions and leaves the step out of `task_memory`; (4) tracing (trace.jsonl + keyframes + narration) and MCP exposure are automatic — `execute()` is the single choke point, and `mcp_server.py` serves `TOOL_SPECS` minus `_EXCLUDED_TOOLS` plus `_EXTRA_TOOLS`; (5) optionally add a regex to `_RULES` in `agent/reflex.py` so the command runs LLM-free (rules are order-sensitive, first match wins, and they drive the real arm with zero LLM involvement — test against `tests/test_streaming_fastpath.py`). One schema source feeds all three consumers (OpenAI-compat, Anthropic, MCP) — a new skill needs zero per-backend work.

**Three-tier dispatch.** `AgentOrchestrator.run_task()` tries tier-1 reflex (regex grammar) then tier-2 experience (hashed-BoW cosine ≥ 0.9 AND wins > losses, persisted in `runs/experience.json`) before ever calling the LLM. The LLM loop ends only via a `task_done` tool call or `max_steps` (default 30); only the FIRST tool call per turn executes (extras get error results back — the OpenAI protocol requires a result per call id). Import the orchestrator as `from cascade.agent.orchestrator import AgentOrchestrator` — `agent/__init__.py` deliberately doesn't re-export it (circular import with `skills.runtime`).

**Motion safety path.** Skills only ever hold a `SafeArm`; raw arm objects never leak upward. `SafeArm.move_joints()` auto-stretches duration to keep min-jerk peak velocity under the cap, checks perception freshness once at `begin_motion()` (deliberately not per-waypoint), then `ArmBase.stream_to()` calls `harness.approve()` on every 50 Hz waypoint — `SafetyViolation` aborts mid-stream. `approve()` has "escape rules" (an arm already outside a limit may move strictly back toward validity); `vet_pose()` is the static pre-motion twin without them. Gripper commands bypass geometric gating (e-stop check only); `SafeArm.raw` is the sanctioned escape hatch for backend-specific ops. Optional obstacle check: `SafetyHarness(occupancy=...)` (`perception/occupancy.py`) adds a `min_clearance_m` gate against a cached obstacle-point cloud — `None` by default (`configs/demo.yaml`'s `occupancy.enabled: false`), refreshed by `WorldWatcher` at `perception_loop.rate_hz` (never per-waypoint — a network round trip inside the 50 Hz stream would itself be a hazard), and treated as "no data, skip the check" (not "blocked") whenever the cache is stale or the bridge (`scripts/serve_occupancy.sh`) is down or unconfigured — same booth-rule shape as the GraspGen-X fallback below. Both wire clients need the `grasping` extra (`pyzmq`, `msgpack-numpy`); without it `OccupancyClient` raises at construction and the map is simply never built. `tests/test_occupancy.py` covers the cache/harness rules with a `FakeClient` AND the protocol itself against a real `serve_occupancy_bridge.py` subprocess — the fake-only half stays green if the bridge changes its response shape, which is why both exist (mutation-checked: renaming the `points` key, dropping region filtering, or a no-op `integrate` each fail only the live-wire tests).

**Grasp pipeline.** `configs/demo.yaml` defaults `grasp.backend: graspgenx` — learned 6-DoF grasps from a ZMQ server (`scripts/serve_graspgenx.sh`, :5556) with **silent fallback to the analytic OBB planner when the server is down** ("booth rule"). Candidates are re-ranked/z-nudged by `GraspOutcomeMemory` (persisted at `~/.cascade/grasp_memory.json`), then `select_grasp()` filters by jaw width, solves IK (pregrasp then grasp), and pre-vets harness geometry including 7 samples along the descent segment. Grasp IK is seeded from `home_q`, not the live pose, and re-homes before the pregrasp on purpose (elbow-down IK branches dip links below the table) — don't "optimize" either away.

**Config system.** `load_demo_config()` loads `configs/demo.yaml` and inserts whole profile dicts under `camera`/`cameras`/`arm`/`llm` — no deep merge between profiles, no env interpolation; the only substitution is `${repo}`/`${assets}` in strings. One exception (2026-07-20): `CASCADE_BOOTH=1` deep-merges `configs/booth.yaml` on top of demo.yaml (booth timing budgets; sets `booth_mode: true`) — `tests/test_config_booth.py` pins the merge. Adding a profile = dropping a YAML file in `configs/{cameras,arms,llm}/`; the `type:` key selects the backend class in the `make_camera`/`make_arm`/LLM factories. `Cfg` is an attr/dict hybrid: attribute access for required keys (raises), `.get()` for optional; the sanctioned runtime-override idiom is writing into `cfg._data` (see `mcp_server.py`). Paths derive from `config.py`'s location, so the package only works from a source checkout.

**Adding a camera/arm backend.** Subclass `CameraBase` (implement `open/close/has_depth/_grab`; `_grab` raises `CameraError` for retryable faults and must NOT set `frame_id` — the watcher dedupes on it) or `ArmBase` (six abstract methods), add a lazy-import branch in `make_camera()`/`make_arm()`, add a profile YAML. Arm profiles pointing at the shipped **reBot** assets MUST carry `joint_signs: [-1]*6` (see contracts below); the SO-101 URDF is authored in the same convention its servos use, so it carries `[1]*5` — the sign list is per asset, not a house style. Heavy imports (pyrealsense2, ultralytics, pinocchio, zmq, mujoco, pyserial, the arm SDK) always stay lazy inside the branch/method that needs them so the mock stack imports nothing heavy.

**The arm's shape is data.** Nothing above the driver layer branches on which robot is attached. `n_joints`, joint limits, `home_q`/`handover_q`, `tool_axis_order`, gripper travel and the reachable envelope all come from the profile. Two mechanisms make that work and both are easy to defeat:
- `SkillRuntime._profile_q(key, what)` fetches joint-space keyframes and **raises** when a profile lacks one or its length disagrees with `n_joints`. Do not reintroduce a default — a 6-element reBot home pose broadcast onto a 5-DoF arm is a bent link, not a wrong answer. Every arm profile must therefore define `home_q` AND `handover_q`.
- An arm profile may carry an `overrides:` block that `load_demo_config` deep-merges over `demo.yaml` (workspace, `table_z`, `max_joint_vel`, grasp budget, `workspace_filter`). This is how one robot's rig geometry stops being everyone's. With `--arms a,b` only the PRIMARY arm's overrides land at top level; each arm's own view is at `cfg.arms[i].resolved`.
- Profiles support `extends: <parent>`, deep-merged (`so101_mock`/`so101_mujoco`/`so101_left`/`so101_right` are all thin children of `so101`). One physical arm, several transports, one place for the kinematic constants. `so101_mjwarp` extends `so101_mujoco` and changes exactly one line — `engine: warp` — so the MuJoCo Warp runtime is exercised against the SAME kinematic/gripper/workspace contract as the C engine; a divergence would mean the two sims describe different arms (`tests/test_mujoco_arm.py::test_mjwarp_profile_is_the_same_arm_on_the_warp_engine`).
- **MuJoCo backend, two runtimes (`control/mujoco_arm.py`).** One `MujocoArm` class, a pluggable engine underneath: `engine: mjc` (default) is the MuJoCo C runtime and the fast path for a single arm (~4.9 µs/step, ~1000× real time on an Apple laptop); `engine: warp` is MuJoCo Warp (`mujoco_warp.step`), driven single-world. MJWarp is a *batched GPU* engine — measured ~3.2 ms/step for one arm on Warp's CPU fallback here (macOS arm64, `warp-lang` reports only `cpu`), ~650× slower than the C engine but still ~1.5× real time, so the GPU path is developable on this Mac and accelerates on the DGX. The engine surface is batch-oriented (whole `qpos`/`ctrl` vectors via `d.ctrl.assign`, not per-joint scalars) because a per-scalar read crosses the host↔device boundary every tick under Warp. `device: auto` degrades CUDA→CPU with a warning, same contract as `device.py`. Physics tests are parametrized over every installed engine, so `[warp]` gets identical coverage to `[mjc]` including a cross-engine agreement check.

**Perception verified against physics, on two engines (`sim/mujoco_rgbd.py`, `tests/test_perception_truth_mujoco.py`, added 2026-08-31).** `grounding._recentre_by_size` corrects the centimetre-scale bias that made grasps catch an object's edge, and it was initially designed and measured **entirely on Isaac** through the bridge on `:8611`, without verification on another engine. `MujocoRGBD` renders offscreen RGB-D from an MJCF on CPU with `data.xpos` as ground truth, which reproduces the measurement on a laptop: **raw fitted OBB centre 2.27 cm mean / 2.64 cm max → de-biased 0.49 cm / 0.59 cm**, independently replicating Isaac's 1.85 → 0.56 cm. Two traps are baked into that module and must not be "simplified" away: (1) the camera must be **declared in the MJCF** — an `MjvCamera` posed from a transform via azimuth/elevation renders depth relative to its *lookat plane*, observed as `reported + true_range = 1.9533` for every range, i.e. depth **decreasing** as the prop moved away; (2) `cam_xmat` is (right, up, **backward**) while `mask_to_points_cam` wants OpenCV (right, **down**, forward). A test also pins the *premise* (the raw bias is really there), so a refactor cannot delete the correction with everything still green.

**Persistent spatial memory (`memory/beliefs.py` `save`/`load`, added 2026-08-31).** The ROADMAP's open item: the world model now survives a restart, so the robot does not re-discover a table it already mapped. The trap is that every belief timestamp is `time.monotonic()`, whose origin **resets with the process** — a naive `json.dump` round-trip yields negative ages and reports a stale belief as `visible`, which is worse than no memory because it looks authoritative. So timestamps are stored as **wall clock** and converted back on load, `LOADED_MIN_AGE_S` floors the apparent age so nothing restored can ever read as `visible`, and `DEFAULT_MAX_AGE_S` (6 h) drops yesterday's table. Writes are atomic (temp + `os.replace`) because the WorldWatcher may be fusing while the save runs. Wired in `build_runtime` (load) and `shutdown_runtime` (save, *first*, before teardown destroys its input); `memory.persist_beliefs: false` or `CASCADE_BELIEFS=0` disables it, `CASCADE_BELIEFS_PATH` relocates it. **A restored belief can never drive the jaws**: `_localize`'s existing `belief_fallback_age_s: 3.0` gate already refuses anything older, and restored beliefs are hours old by construction — they inform the agent, they do not aim it. `tests/conftest.py` has an autouse fixture pinning `CASCADE_BELIEFS_PATH` into `tmp_path`; without it a developer's real run leaks into the suite and a remembered object over empty mock table makes `pick_and_place` fail and escalate to the LLM (this actually happened, and surfaced as an unrelated-looking "LLM must not be called on the reflex path").

**Curriculum decomposition + warm start in the fast tier (`agent/reflex.py`, added 2026-08-31).** Agentic-VLA (arXiv:2605.22896) decomposes a task into sub-goals and warm-starts each from retrieved experience; its trainable parts (synthesized rewards, online policy updates) do not transplant to a frozen brain with analytic skills, but the *structure* does. `FastPlanner` now resolves reflex → experience → **curriculum**: `split_subgoals` splits on explicit sequence connectives only (`and then`/`then`/`luego`/`después` — never a bare "and", which would cut a single `pick_and_place` into two half-motions), then each clause is planned by experience recall first (that preference *is* the warm start) and the grammar second. This is **all-or-nothing**: one unplannable clause abandons the whole plan to the LLM, because running the prefix and reporting success is exactly the failure `parse_command`'s compound-command refusal exists to prevent. `FastPlan.subgoal_call_counts` records each clause's span at build time (a clause can compile to several calls, so re-deriving the split later mis-credits outcomes), and the orchestrator credits each sub-goal separately via `note_subgoal_outcome` — which is what makes it compound: a clause proven inside one sequence warm-starts any later task containing it. Net effect: "wave and then go home" no longer costs an LLM turn.

**Device agnosticism.** `cascade/device.py` is the only module that decides where a model runs: `resolve_device(spec)` maps `auto` to CUDA/ROCm → Apple MPS → CPU by probing the host, and an EXPLICIT device the host lacks degrades to the best available with a warning rather than raising (configs travel between machines; same booth rule as the GraspGen-X and occupancy fallbacks). `CASCADE_DEVICE` overrides every profile at once. Consumers: `perception/detector.py`, `perception/segmenter.py` (resolved lazily on first use — probing torch costs seconds), and the LIBERO baseline runner. **torch is deliberately not a dependency**: the correct build is per-platform. A hardcoded `cuda` in `configs/` fails `tests/test_device_and_profiles.py`.

**Feetech/SO-101 transport.** `control/feetech.py` is the pure wire protocol (framing, checksum, byte order, SYNC_WRITE) and is exhaustively unit-tested against a fake port; `control/feetech_arm.py` is the arm on top and is **UNVERIFIED on hardware**. Every register constant is an `(address, width)` tuple and all builders take that tuple, so an address can never be paired with the wrong width. Streaming uses one SYNC_WRITE frame per waypoint (N individual writes would blow the 20 ms budget on USB latency); reads are per-servo, so never add a read to the waypoint path. `stop()` is soft (re-assert the hold pose, torque ON); `hard_estop()` cuts torque and the arm FALLS. `scripts/diag_so101.py` is the read-only pre-flight plus a single-joint jog that reports which `wire_signs` entry to flip — it imports nothing from the package's config layer, because it exists for the case where the profile is wrong.

**MCP server.** `apps/mcp_server.py` is hand-rolled newline-JSON-RPC over stdio, and `stream_server.py` serves HTTP with stdlib `http.server` — both deliberate; don't add MCP-SDK or web-framework deps here. It hardcodes `llm='mock'`: the MCP host is the brain and the built-in tiers are bypassed. Stdout discipline: protocol frames only — every runtime call is wrapped in `redirect_stdout(sys.stderr)`, and since 2026-07-20 a stdin reader thread + worker queue share stdout, so protocol writes must go through `main()`'s locked `_send`; new code on this path must never print to stdout. The reader thread is the stop channel: `emergency_stop` frames are answered out-of-band mid-tool-call (a stop during the runtime build is held in `_stop_pending` and applied when the build finishes — never lost), `notifications/cancelled` on an in-flight `_MOTION_SKILLS` call latches the e-stop (guarded by `_cancel_lock` against the check/publish race), `ping` is answered from the reader, first SIGINT freezes (second exits — torque off), SIGUSR1 clears the e-stop (the staff reset channel), and `CASCADE_HIDE_TOOLS` delists+rejects tools (`emergency_stop` is never hideable; booth sessions hide `reset_stop`). Regression pins: `tests/test_review_regressions_v4.py`. The dashboard STOP button AND chat box are wired in MCP mode inside `_ensure_runtime` — don't remove them assuming demo.py's wiring covers it (that only runs in the CLI); the MCP-mode chat is reflex-grammar-only by design (`_reflex_chat_task`, the booth's host-outage fallback), never an LLM.

## Contracts & invariants

- **Units/frames:** positions in meters in the ROBOT BASE frame; joints in radians (degrees only in `get_observation` output). Base frame: +x away from robot, +y left; "left" = larger y. OBB convention everywhere: extents sorted descending, axes as matrix columns — never use `extent[2]` as height, use `top_z`.
- **`Frame.rgb` is BGR** (OpenCV) despite the name; `depth_m` is float32 metric meters aligned to color, 0 = invalid. `Frame.size` is (w, h) — opposite of numpy shape.
- **In-process perception/state timestamps (`Frame.t`, `RobotState.t`, belief ages, `MemoryEvent.t`) are `time.monotonic()`**, never wall clock — belief freshness math depends on it. Persisted/reporting timestamps (trace.jsonl, grasp-memory `saved_at`, dashboard status) are wall-clock `time.time()`; never mix the two.
- **Joint mirror:** both shipped RS assets (URDF and USD) are authored mirrored (`q_asset = -q_local`); `joint_signs` bakes the flip in at model load, so every constant in the repo (home_q, limits, tuned poses) is LOCAL. `tests/test_usd_model.py` pins URDF↔USD kinematic identity — it fires if you touch `assets/`. The SO-101 URDF is NOT mirrored (`[1]*5`), and there is a trap if you ever need a `-1` there: `usd_model.apply_joint_signs` walks joints in DOCUMENT order, and that URDF declares them backwards (gripper first, shoulder_pan last) relative to Pinocchio's kinematic order, so a sign list would be applied to the wrong joints. All-ones never invokes that path.
- **`n_controlled` slices the FIRST N joints** and zero-pads the rest, which is only correct because both shipped URDFs order the arm chain before the gripper. That is a property of the file, not a guarantee — check it for any new model (`tests/test_kinematics_so101.py::test_arm_chain_occupies_the_first_five_joint_indices` pins it for SO-101; the SO-101 URDF's *declaration* order is reversed and only Pinocchio's root-down traversal saves it).
- **`ik_task_weights` exists but no shipped profile uses it**, including the 5-DoF SO-101. Its wrist-roll axis is collinear with the tool approach (measured: rolling it turns the approach by 0.00°), so for a vertical approach the roll spends itself on jaw yaw and unweighted 6-DoF IK solves its workspace at 0.94. Do not "fix" the 5-DoF case by zeroing a weight — that makes IK accept poses the wrist cannot hold, replacing an honest failure with a silent wrong one.
- **`tool_axis_order` is per arm and not guessable.** `down_open` = columns [approach, open, third] (reBot); `open_down` = [open, third, approach] (LIBERO Panda AND the SO-101, whose fingers run along col2 with the jaws separating along col0 — measured from the Menagerie fingertip geometry). Getting it wrong rotates every grasp 90°, which is a past defect on the Panda path, not a hypothetical.
- **Error taxonomy** (`types.py`): `SkillError` = failure the agent should reason about; `SafetyViolation` = harness rejection; plain `RuntimeError` = crash/hardware fault. The orchestrator's recovery logic depends on this split.
- **Invariant:** `Kinematics.ik` `limit_margin` (0.025 rad) must stay strictly greater than the harness `joint_margin` (0.02) so IK never returns a pose the harness rejects — a past review defect; never tune one side alone.
- **Booleans from LLMs arrive as strings** ("false" is truthy) — coerce with the `_as_bool` pattern in any new skill taking a bool.
- Gripper `open_pos`/`closed_pos` polarity differs per arm profile (RS: open=-6.8 rad; mock: 0→1; Isaac: fraction, open=1.0; SO-101: open=+0.60 rad, closed=-0.15) — compute travel as `closed_pos - open_pos`, sign-aware.
- The SO-101 jaw is a single-hinge "beak", not parallel fingers: opening is NOT linear in motor angle and the jaw retracts along the approach as it opens. `max_width_m` is deliberately below the geometric gap at `open_pos` because the framework's angle→width map is linear; see the measured table in `configs/arms/so101.yaml`.
- Comments in control/safety/grasping encode rig-verified failure observations that tests rely on — preserve and extend them.

## Testing conventions

- Shared surface in `tests/conftest.py`: `REPO`, `URDF`, `USD`, `SO101_URDF`, `JOINT_SIGNS`, `has_pinocchio()`, `needs_pin`, `needs_pin_so101`, and the `demo_cfg` / `rng` fixtures. Import via `from conftest import ...`.
- **Do not gate a test on an optional extra when the contract can be faked.** CI installs neither `openai` nor `anthropic`, so an `importorskip("openai")` means the assertion runs on NO machine; `tests/test_hermes_brain.py` injects a stub `openai` module into `sys.modules` instead and asserts what the client was constructed with. Reserve real skips for things a stub cannot stand in for (physics, hardware).
- Tests that fake the host for `device.py` must clear the `lru_cache` on `_torch`/`best_device` at BOTH ends, and defensively (monkeypatch's finalizer runs after the fixture teardown, so `_torch` may already be a plain function by then).
- E2E tests build the real pipeline with `build_runtime(cfg, tmp_path / "run")` on the mock stack; control grasp outcomes with `arm.object_stop_frac` (0.5 = object held, None = air-grasp). Always poll `runtime.beliefs.find("red object")` in a deadline loop before executing skills (the watcher thread needs a detector pass), and tear down with try/finally `shutdown_runtime(runtime, arm)` — these are real threads.
- Config overrides in tests mutate `cfg._data[...]` directly; there is no override API. Servers bind port 0; `MockLLM(responses)` plays a script and records `.requests` for assertions. **Build HTTP test URLs from `conftest.loopback_host()`, never a literal `127.0.0.1`** — with some VPN clients active (a `utun` interface holding 198.18.0.0/24) a connection to the numeric loopback is intercepted and closed (`RemoteDisconnected`) while `localhost` works, which made six streaming/live-view/MCP tests fail for reasons unrelated to the code.
- `tests/test_review_regressions*.py` is a convention: each adversarial review pass gets its own file, one test per confirmed defect with the root cause in the docstring (v1 = 2026-07-16 review, v2 = livestreaming review, v3 = late pins for v1 fixes). New review findings get their own vN file, not silent folding into feature tests.
- `scripts/` modules are tested via `sys.path.insert(0, str(REPO / "scripts"))`, not packaging.

## Gotchas

- **Stale absolute paths from the original dev boxes** remain in: `configs/arms/rebot_rs.yaml` `sdk_path`, `setup_env.sh`, and the asset-provenance scripts under `assets/usd/RS-rebot-dev-arm/scripts/` (records of how the USD was produced — leave those). (`.mcp.json`, `setup_agents.py`, `hermes_demo.sh` and `dashboard_runner.py` were fixed 2026-07-20 — the latter two now derive paths from the script location.) New runner scripts should take the interpreter from an env var/flag like `setup_agents.py` does, and prefer the `install_hermes.sh` resolution order: `$PY`, then `<repo>/.venv`, then `<repo>/../.demo`, then `python3`.
- **`--llm` defaults to `auto`**, which resolves to `hermes` / `anthropic` / `openai` if the matching key is exported, else `mock`. A test or benchmark that must stay offline has to pass `--llm mock` explicitly (or clear the keys); `tests/test_hermes_brain.py` scrubs them in an autouse fixture so the suite certifies the same behavior on a credentialled dev box and in CI. `CASCADE_LLM` overrides the flag.
- **Persistent learned state outside the run dir** changes behavior between otherwise-identical sessions: `~/.cascade/grasp_memory.json` and `runs/experience.json` (tier-2 habits replay stored call lists verbatim at cosine ≥ 0.9). Delete both for deterministic/fresh demo behavior.
- YOLOE's text encoder (`mobileclip*.ts`) resolves relative to the CWD — wrong launch directory makes all detections silently vanish. In this checkout the encoder lives at `models/mobileclip_blt.ts`, so launch from `models/` (see `scripts/record_demo.py`) or copy the `.ts` into your launch directory; the detector weights themselves are fine anywhere (`${repo}/models/...` is absolute). Set `YOLO_OFFLINE=True` or ultralytics phones GitHub and stalls the watcher.
- Never use `==`/`list.remove()` on `ObjectBelief` — its numpy fields make dataclass `__eq__` raise ("truth value of an array is ambiguous"); remove by identity as `BeliefStore.mark_removed` does.
- Accessing any backend-specific attribute on a LazyArm-backed `SafeArm.raw` — anything not on LazyArm's own surface, including `hasattr` probes for extras like `close_gripper_two_stage` — materializes the arm and **powers the motors** via `__getattr__`. LazyArm's own members (`connected`, `stop`, `disconnect`, `resume`) are safe to touch; never probe backend capabilities outside a motion context.
- Beliefs are keyed by DETECTOR labels, not user-query words — `CASCADE_DETECT_CLASSES` must name what users will ask for ("banana" can't resolve a belief labeled "fruit").
- `DepthProvider.ensure_depth` mutates the shared `Frame` in place; plane-cast depth is only valid for pixels on the table (tall-object heights are wrong on RGB-only cameras).
- Ctrl+C in the demo CLI is a soft stop (freeze + e-stop latch, no free-fall); the second Ctrl+C exits. Park the arm (`move_home`) before disconnecting the RS **or** SO-101 arm — `disconnect()` disables torque and a loaded arm falls. (The SO-101's `home_q` parks it low for exactly this reason; do not "improve" it into a raised pose.)
- The mock camera re-renders an identical frame every grab, so it declares `static_scene: true` and the visual postcondition channel ABSTAINS. Without that, any place whose source and target both project into frame reports "the scene looks untouched" and fails — the reBot only escaped it because its drop zone lands out of view. Do not remove the flag to "get more verification": it produces false negatives, not evidence.
- Isaac: `isaac_bridge.py` must run under Isaac Sim's `python.sh`, `SimulationApp` before any omni import, engine stays `physx` (Newton NaNs at grasp contact); `CASCADE_PHYSICS_DEVICE=cpu` is the escape hatch for GPU-PhysX boot NaNs. The bridge binds 127.0.0.1 because its `exec` op is arbitrary code execution.
- Base deps pin `opencv-python-headless` — `cv2.imshow` viewers only work because the rig venv has full opencv; in a fresh install they silently degrade.
- `assets/mjcf/` is gitignored and fetched. Anything that needs it must degrade with a message naming `scripts/fetch_robot_assets.py`, never assume it — CI has no meshes and neither does a fresh clone. The vendored URDF is enough for FK/IK, the safety layer and every non-physics test.

## Doc status (README / ARCHITECTURE / QUICKSTART / ROADMAP synced to code on 2026-09-10)

- **Spark delivery update:** `docs/SPARK_DELIVERY.md` is the current installation/acceptance boundary. `bootstrap.sh` forwards to the Spark-default installer; Isaac is 6.1.0.0 / Python3.12, Newton is already the bridge default, Cosmos3-Edge is the required local brain, and OpenClaw2026.9.3 uses a separate CLI prefix/profile. EULA consent is explicit; no driver replacement. The Mac's MuJoCo/OpenAI proof and standalone Newton CPU tests do not certify Isaac/Cosmos GPU operation. Publication and a clean Spark rehearsal remain pending. README/QUICKSTART now lead with this delivery path rather than pretending auto-detection installs Isaac. Historical test totals below are not a final delivery measurement.
- **Reference refusals:** `ReferenceResolutionError` must survive `_localize` retries and secondary-camera fallbacks. A remembered noun cannot resolve ordinal/exclusion/relative-size identity after all current observations miss; preserve plain-noun flicker fallback, but ask for clarification on constrained references instead of silently choosing another object.
- **Mobility / navigation (2026-09-10, design only):** `docs/MOBILITY_AND_NAVIGATION_DESIGN.md` is the approved direction (G1/H1 in Isaac first; `MobileBase` twin of `ArmBase`; Nav2 or Warp planner behind one interface; Vesta's pixel-goal / turn / stop as skills). No code exists for it yet: do not describe cascade as having a mobile base, navigation, mapping or self-localization -- ROS2 (`ros2_arm.py`) and Unitree (`unitree_arm.py`) drive ARMS, and "localization" in this repo means object grounding. ARCHITECTURE has a section stating exactly this.
- **Full doc sync 2026-09-10 (evening):** `docs/ARCHITECTURE.md` rewritten around the runtime as it is now (the `execute()` choke point step by step, verification channels, the shared sim world as an instrument, the memory-store table, evaluation, launch/hosts, verification status, and a "Counts" recipe); README's header diagram now shows the verify-on-an-independent-channel loop and the 33/41 numbers, the skills table gained `turn_screw`, the docs index lists every file under `docs/`; `docs/QUICKSTART.md` now leads with `./run.sh`, the one-session `openclaw agent -m ... --session-id` flow, the memory demo, and a troubleshooting list built from the real failures of the last two days (Connection closed = server died mid-call; 60 s host timeout → e-stop; dead MCP entries). Stale everywhere before: 30/37 counts, `ss`, `openclaw_demo.sh` as the primary path, 224/480 tests. `docs/ARCHITECTURE.md` no longer references `scripts/serve_nvblox*.sh` (removed; the occupancy bridge is `serve_occupancy.sh` → `serve_occupancy_bridge.py --backend auto`).

- **New since the last sync (2026-09-10):** the Vesta memory harness (`memory/episodic.py::memory_frames`, injected per LLM turn by `orchestrator._with_memory_harness`, served to chat hosts by the `task_memory` MCP tool), the two-prop scene (`configs/cameras/mujoco_scene_two.yaml`, `extra_props:`; `sim/demo_scene.py` writes N props, `MockDetector(extra_labels=)` finds N colours), `reset_scene` (skill + reflex; `MujocoWorld.reset_props`, `BeliefStore.clear`), and the launcher's post-proof reset. `launch.sh` registers the MCP server with `requestTimeoutMs: 300000` (the OpenClaw default of 60 s cancelled persistent picks and latched the e-stop), prunes OpenClaw MCP entries whose command no longer exists, and `--down` kills the per-session `cascade.apps.mcp_server` processes the gateway never reaps. Rendered cameras pump at `fps: 10` (30 burned ~85 % of a core idle). `CASCADE_MJ_VIEW=1` opens the MuJoCo window (the arm profile's `view: false` was never overridden before, so the banner's promise was empty) -- guarded by `_display_unavailable_reason()`: with the display asleep/locked `CGGetActiveDisplayList` is empty and `launch_passive` SEGFAULTS the whole MCP server mid tool call, so the engine skips with a reason and retries on later motion; the cv2 camera window is skipped up front on macOS/headless-opencv with a one-line reason instead of an opaque C++ exception per run.
- Counts re-derived 2026-09-10: `pytest tests/ -q` → 737 passed / 2 deselected (hardware) / 0 skipped on macOS (extras sim, sim-warp, grasping, occupancy, llm); `mcp probe cascade --json` lists 41 tools (34 `TOOL_SPECS` − `task_done` + 8 `_EXTRA_TOOLS`, the eighth being `task_memory`). `ruff check src/ scripts/*.py tests/ --select F,E9` is clean (it was not: one real `F821` undefined name in `runtime.py`).
- `--check` is READ-ONLY by contract (`launch.sh`): every install/fetch step needs a `CHECK == 1 && SETUP == 0` warn-branch; it mutated the venv once (torch) because two steps lacked it. Verify with a `uv pip list --format=freeze` diff around a `--check` run.
- Postcondition verifier failures are UNVERIFIED postconditions, never swallowed (`SkillRuntime.execute`, guarded by `tests/test_verifier_crash.py` incl. a static check that the `except: pass` did not return).
- Belief-store rule since 2026-09-10: two observations with DIFFERENT measured colours are two objects however close (proximity fusion is for label aliases of one object). Every belief-writing path must pass `color=detection_color(...)` or the rule cannot fire there — `_update_beliefs_from_frame` did not and fused the two cubes.
- Mock detector contract since 2026-09-10: `detect(frame, classes=None)` is OPEN WORLD and resets any narrower vocabulary a previous call set — same as the real detector. A colour word in `classes` selects that colour's prop only.

- **New since the last sync (2026-09-09):** the MuJoCo demo gained an independent verification channel and a one-click launcher. `sim/mujoco_world.py` (one shared `MjModel/MjData` per MJCF path, refcounted, one lock — the arm's `_MjcEngine`, the rendered camera and the truth reader all attach through `acquire()/peek()`); `perception/mujoco_camera.py` (`type: mujoco`, RGB-D rendered from the live world; renderer owned per Thread OBJECT, not thread id — pthread ids recycle and a recycled id inherits a dead thread's locked CGL context and hangs forever); `configs/cameras/mujoco_scene.yaml` (prop at base (0.20, +0.10): the old (0.20, 0) sits under the SO-101's home TCP, fully occluded top-down); `sim/truth.py` `MujocoTruthReader` + `LazyTruthPoseFn` (binds on first use; can bind through the camera's world before the lazy arm exists); `agent/effects.py` `_comparable_start` (a displacement must be two readings of the SAME channel — the chat path mixed a disk-restored belief with physics and refuted a good pick); `sim/demo_scene.py` `resolved_scene_path()` (single derivation of the MJCF a MuJoCo arm loads; the loader plants `mj_scene`/`mj_scene_source` on rendered cameras so they can regenerate the scene when the lazy arm has not written it yet); `control/lazy_arm.py` `profile_type` (readable without materializing). `scripts/launch.sh` is the one-click entry (`--sim auto|isaac|mujoco|none`, `--dry-run`, `--down`); verified on OpenClaw 2026.9.3 (= 2.0): `mcp set` is the idempotent form, plain `mcp probe` prints only a count (use `--json`), `agent exec` takes the message positionally, `update` must run with the gateway stopped, and on macOS the MCP server runs under `mjpython` or the viewer never opens. `docs/ROADMAP.md` has the "Landed 2026-09-09" section with the SOTA delta (Harness-VLA v4, ASPIRE + Pigey code public, Waddle stack) and follow-ups #9–#13.
- Counts re-derived 2026-09-09: `pytest tests/ -q` → 639 passed / 2 deselected (hardware) / 0 skipped on macOS with the `sim` extra; `mcp probe cascade --json` lists 39 tools (38 + `list_arms`).
- **New since the last sync (2026-08-29):** the framework was generalized off its reference hardware — and off a single arm. `control/arm_rig.py` (`ArmRig`, the twin of `CameraRig`: N named arms, `--arms a,b`, per-arm `SafetyHarness`), inter-arm proximity gating via `base_pose` + `safety.neighbor_clearance_m`, an optional `arm="<name>"` on every motion skill plus `list_arms`, and `perception/calibration.py` + `scripts/calibrate_camera.py` (Kabsch camera→base extrinsic with RMSE/degeneracy refusal) — all four are described under "Architecture" above. Also: `cascade/device.py` (auto CUDA/ROCm/MPS/CPU, `CASCADE_DEVICE`); `control/mujoco_arm.py` (any MJCF, hardware- and GPU-free physics); `control/feetech.py` + `control/feetech_arm.py` (SO-101 over a Feetech serial bus, **unverified on hardware**) with `scripts/diag_so101.py`; `assets/urdf/so101/` vendored (URDF text only, with PROVENANCE) plus `scripts/fetch_robot_assets.py` for MJCFs/meshes at a pinned commit; profile `extends:` and arm `overrides:` in `config.py`; `configs/cameras/mock_small.yaml` (a scene a 55 mm jaw can actually pick); Hermes/Nous Portal as the default brain (`configs/llm/hermes.yaml`, `--llm auto`, `scripts/install_hermes.sh`) alongside its longstanding role as the default MCP host; new extras `arm-feetech` and `sim`; CI now a 3-architecture matrix plus a `minimal-install` job.
- The SO-101 profile's numbers were re-derived from the MJCF/URDF on 2026-08-29 and an earlier revision was WRONG in ways worth knowing about, since the same mistakes are easy to repeat for the next arm: `tool_axis_order` named the wrong column as the approach (so `home_q` was a forward-facing pose, not top-down), and `yaw_from_base`/`ik_task_weights` encoded a "parallel wrist axes pin the yaw" claim that measurement refuted. `tests/test_kinematics_so101.py` now pins each surviving claim with the number the profile quotes.
- Counts go stale fast — before citing them, re-derive: test count via `pytest tests/ -q --collect-only`, skill count via `TOOL_SPECS` in `skills/runtime.py` (32 specs = 31 skills + `task_done`; MCP = 31 + 7 extras (`_EXTRA_TOOLS` in `apps/mcp_server.py`: `camera_snapshot`, `world_state`, `live_view_url`, `emergency_stop`, `reset_stop`, `robot_knowledge`, `verify_last_action`) = 38 tools by default — `CASCADE_HIDE_TOOLS` subtracts at runtime). Re-derived 2026-08-28 (`list_arms` added with the arm rig; previously 31 specs / 37 tools).
- **New since the last sync (2026-08-27):** `src/cascade/perception/occupancy.py` adds an optional nvblox-style occupancy/collision map (`OccupancyMap`, wired into `SafetyHarness` as `occupancy=`) — see "Motion safety path" below. its bridge is now `scripts/serve_occupancy_bridge.py` (+ `serve_occupancy.sh`), `--backend auto`: real `nvblox_torch` on NVIDIA GPUs, else the Warp TSDF/EDT kernels in `perception/occupancy_backends.py`, else numpy voxels (the original Open3D `serve_nvblox*` bridge was replaced on 2026-09-09). `scripts/install_occupancy_backend.sh` and `scripts/bootstrap.sh` (curl\|bash-able, the latter chains OpenClaw CLI install + `serve_cosmos_vllm.sh` + `openclaw_demo.sh`) are new one-shot setup entry points, documented in README's "Install" section.
- Easy to be misled by: episodic-memory embeddings (`embed_dim`) are written and tested but **not wired** into the shipped demo; the only live TurboQuant user is tier-2 ExperienceMemory. (`skills/library.py` IS wired since 2026-07-31: `aspire.distil` writes notes after a run, `aspire.retrieve` loads guard-matched notes into the tier-3 context in `orchestrator.run_task`; older notes in this file and in ARCHITECTURE that called it "store only" were stale until 2026-09-10.)
- `configs/llm/local_qwen.yaml` was reconciled with the serve scripts on 2026-07-20 (`model: Qwen/Qwen3.6-27B`). The sync rule stands: llama.cpp ignores the request's model name, vLLM rejects a mismatch — if `scripts/serve_qwen_*.sh` change `MODEL`/`HF_REPO`, update the profile's `model:` (and `supports_vision:`).
- `.mcp.json` uses `python3` from the active `PATH` with `PYTHONPATH=src` and pins the Isaac profiles. Start the MCP host from the repository root with the cascade environment active, or generate a local configuration with an explicit `--python` interpreter. `setup_agents.py` derives `DEFAULT_PY` from the repo location (standard `…/Projects/demo/.demo` layout; pass `--python` otherwise). Regenerating REPLACES the whole cascade entry with the flags given — defaults are `--camera l515 --arm mock`, so to keep this file's behavior pass `--camera isaac,isaac_side --arm isaac`. Since 2026-07-20 the env landmines have flags: `--detect-classes` (CASCADE_DETECT_CLASSES), `--hide-tools` (CASCADE_HIDE_TOOLS), repeatable `--env KEY=VALUE`, and `YOLO_OFFLINE`/`ULTRALYTICS_OFFLINE` are emitted by default (`--no-offline` to opt out) — so the checked-in Isaac entry, regenerated without `--no-offline`, now gains the offline vars (harmless).
- `docs/BOOTH_RUNBOOK.md` (added 2026-07-20) is the attendee-session script for the live rig; `scripts/booth_up.sh` (pre-flight, catches the silent killers) and `scripts/booth_reset.sh` (between-groups reset + grasp-memory keep/wipe/restore policy) are its ops entry points. The runbook's config-tuning table (§1) is applied via the `CASCADE_BOOTH=1` overlay (`configs/booth.yaml`), never by editing `configs/demo.yaml` — dev keeps dev values; an autouse conftest fixture scrubs ambient `CASCADE_BOOTH` so the suite always certifies dev behavior.
