# Provenance

- Upstream: `unitreerobotics/unitree_ros`
- Commit: `7d6075f7f58588b189b940130e3edab3c839b2df` (master HEAD, 2026-09-03)
- File: `robots/h1_description/urdf/h1.urdf` -> `h1_full.urdf`
- License: BSD-3-Clause (repository LICENSE)
- `h1_right_arm.urdf` is DERIVED, not upstream: generated by
  `python scripts/extract_arm_urdf.py h1_full.urdf --root torso_link \
      --tip right_elbow_link --name h1_right_arm --tcp-offset 0.28,0,-0.015`
  The H1 (gen 1) arm is 4 DoF and has NO wrist and NO hand: the last link IS
  the forearm. `tcp_link` sits at the forearm tip — the [0.28, 0, -0.015]
  offset is the far end of the Menagerie collision capsule
  (`fromto="0 0 0 0.28 0 -0.015"` + end sphere) for `right_elbow_link`.
- Why derived: cascade's kinematics command the FIRST n_controlled q indices
  (control/kinematics.py); the full humanoid URDF orders floating base and
  legs first, so an arm profile must load an arm-only document whose chain
  is exactly torso->tip in order.
- Full-model file is kept verbatim for re-derivation and review.
- Re-vendor: `gh api repos/unitreerobotics/unitree_ros/contents/robots/h1_description/urdf/h1.urdf?ref=<commit> -H "Accept: application/vnd.github.raw"`
  (upstream is CRLF; the raw API serves the blob as-is — normalize if diffing)
