# Provenance

- Upstream: `agilexrobotics/piper_ros`
- Commit: `ac41fcbcdda598f01b51cf6175ed9a24d0dacadc` (branch `noetic` HEAD, 2026-09-03)
- File: `src/piper_description/urdf/piper_description.urdf` -> `piper.urdf`
- License: MIT (repository license)
- Note: upstream file is CRLF; vendored bytes are the upstream blob served
  with `Accept: application/vnd.github.raw` (LF). Verified: content identical
  after newline normalization.
- Mesh references are `package://piper_description/meshes/*.STL` — inert for
  kinematics (Pinocchio never opens meshes). Rendering/physics use the MuJoCo
  Menagerie `agilex_piper` MJCF fetched by `scripts/fetch_robot_assets.py`.
- Chain (nq=8): joint1..joint6 revolute (arm, FIRST — the `n_controlled`
  slice contract holds), joint7/joint8 prismatic gripper fingers
  (0..0.035 m each, mirrored).
- `piper_arm.urdf` is DERIVED, not upstream: generated by
  `python scripts/extract_arm_urdf.py piper.urdf --root base_link \
      --tip gripper_base --name piper_arm --tcp-offset 0,0,0.15`
  Arm-only (joint1..joint6 + the fixed gripper_base): the prismatic fingers
  are excluded so the arm chain occupies ALL q indices. `tcp_link` sits
  0.15 m along gripper_base's +z — between the finger-pad roots (0.1358 m)
  and the tips, DERIVED from the piper_ros URDF finger origins, not measured.
- Re-vendor: `gh api repos/agilexrobotics/piper_ros/contents/src/piper_description/urdf/piper_description.urdf?ref=<commit> -H "Accept: application/vnd.github.raw"`
