# Local backend on the DGX Spark: Qwen3-VL behind an OpenAI-compatible server
# (llama.cpp llama-server or vLLM). Multimodal (vision) + tool-calling.
# Start it with scripts/serve_qwen_llamacpp.sh or scripts/serve_qwen_vllm.sh.
type: openai_compat
base_url: http://127.0.0.1:8080/v1
model: Qwen3VL-30B-A3B-Instruct-Q4_K_M
api_key: EMPTY
supports_vision: true     # Qwen3-VL is a unified VLM
temperature: 0.2
